const popup = document.getElementById('reportModalLabel')
const report_modal = document.getElementById('reportModal')

report_modal.addEventListener('click', () => {
    popup.style.display = 'block'
})